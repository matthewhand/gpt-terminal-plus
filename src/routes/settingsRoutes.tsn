import express, { Request, Response } from 'express';
import Debug from 'debug';
import { checkAuthToken } from '../middlewares/checkAuthToken';
import { getRedactedSettings, getFullSettings } from '../config/convictConfig';
