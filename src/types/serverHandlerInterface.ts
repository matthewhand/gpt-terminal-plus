import { SystemInfo } from './systemInfo'; export interface ServerHandlerInterface { setCurrentDirectory(directory: string): boolean; createFile(directory: string, filename: string, content: string, backup: boolean): Promise<boolean>; updateFile(filePath: string, pattern: string, replacement: string, backup: boolean): Promise<boolean>; amendFile(filePath: string, content: string): Promise<boolean>; getSystemInfo(): Promise<SystemInfo>; listFiles(directory: string, limit?: number, offset?: number, orderBy?: string): Promise<{ items: string[], totalPages: number, responseId: string }>; executeCommand(command: string, timeout?: number, directory?: string): Promise<{ stdout: string; stderr: string }>; }
