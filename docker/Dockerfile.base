FROM node:18-slim

WORKDIR /app

RUN apt-get update && \
    apt-get install -y python3 python3-pip python3-venv && \
    apt-get clean

COPY docker/check_api_token.sh /usr/local/bin/check_api_token.sh
RUN chmod +x /usr/local/bin/check_api_token.sh
RUN /usr/local/bin/check_api_token.sh

EXPOSE 5004