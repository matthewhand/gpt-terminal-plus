const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); async function getTagId(tagName) { try { const query = 'query { tags { id name } }'; const res = await monday.api(query); const tags = res.data.tags; const tag = tags.find(t => t.name.toLowerCase() === tagName.toLowerCase()); if (tag) { console.log('Tag ID for', tagName, ':', tag.id); return tag.id; } else { console.log('Tag not found:', tagName); return null; } } catch (error) { console.error('Failed to retrieve tags:', error); } } getTagId('Sci-Fi');
