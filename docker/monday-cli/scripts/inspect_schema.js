const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const typeName = process.argv[2] || 'Board'; async function inspectSchema() { try { const query = '{ __type(name: "' + typeName + '") { name fields { name type { name kind ofType { name kind } } } } }'; const res = await monday.api(query); console.log('Schema for', typeName, ':', JSON.stringify(res.data.__type, null, 2)); } catch (error) { console.error('Failed to retrieve schema:', error); } } inspectSchema();
