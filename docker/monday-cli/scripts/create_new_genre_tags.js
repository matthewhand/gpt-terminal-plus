const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const newTags = ['Historical Comedy', 'Mystery-Comedy', 'Procedural Drama']; async function createTags() { try { for (const tag of newTags) { const mutation = 'mutation { create_tag (board_id: 2809374317, tag_name: "' + tag + '") { id name } }'; const res = await monday.api(mutation); console.log('Created tag:', res.data.create_tag.name, 'with ID:', res.data.create_tag.id); } } catch (error) { console.error('Failed to create tags:', error); } } createTags();
