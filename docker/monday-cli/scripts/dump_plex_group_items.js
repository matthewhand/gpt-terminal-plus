const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; const groupId = 'new_group81872'; async function dumpGroupItems() { try { const query = 'query { boards(ids: [' + boardId + ']) { items { id name group { id } column_values(ids: ["comments"]) { text } } } }'; const res = await monday.api(query); console.log('Full API Response:', JSON.stringify(res, null, 2)); const items = res.data.boards[0].items.filter(item => item.group.id === groupId); if (items.length === 0) { console.log('No items found in the "Plex" group.'); return; } const itemsWithoutComments = items.filter(item => !item.column_values[0].text); if (itemsWithoutComments.length === 0) { console.log('All items have comments.'); return; } itemsWithoutComments.forEach(item => { console.log('Item without comments: ' + item.name + ' (ID: ' + item.id + ')'); }); } catch (error) { console.error('Failed to retrieve items:', error); } } dumpGroupItems();
