const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const newTag = 'Historical Comedy'; async function createSingleTag() { try { const mutation = 'mutation { create_tag (board_id: 2809374317, tag_name: "' + newTag + '") { id name } }'; const res = await monday.api(mutation); if (res.data && res.data.create_tag) { console.log('Created tag:', res.data.create_tag.name, 'with ID:', res.data.create_tag.id); } else { console.log('Failed to create tag, response:', JSON.stringify(res)); } } catch (error) { console.error('Failed to create tag:', error); } } createSingleTag();
