const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; const groupId = 'new_group81872'; async function getItemsInGroup() { try { const query = 'query { boards(ids: [' + boardId + ']) { groups(ids: ["' + groupId + '"]) { items { id name column_values(ids: ["comments"]) { text } } } } }'; const res = await monday.api(query); console.log('Group Items:', JSON.stringify(res.data, null, 2)); const items = res.data.boards[0].groups[0].items; const itemsWithoutComments = items.filter(item => !item.column_values[0].text); if (itemsWithoutComments.length === 0) { console.log('All items have comments.'); return; } itemsWithoutComments.forEach(item => { console.log('Item without comments: ' + item.name + ' (ID: ' + item.id + ')'); }); } catch (error) { console.error('Failed to retrieve items:', error); } } getItemsInGroup();
