const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; const groupId = 'new_group81872'; const genre = 'Sci-Fi'; async function updateItemGenres() { try { const query = 'query { boards(ids: [' + boardId + ']) { groups(ids: ["' + groupId + '"]) { items_page { items { id name column_values(ids: ["genre"]) { text } } } } } }'; const res = await monday.api(query); const items = res.data.boards[0].groups[0].items_page.items; const genrelessItems = items.filter(item => item.column_values.length === 0 || !item.column_values[0].text); for (const item of genrelessItems) { const mutation = 'mutation { change_column_value(board_id: ' + boardId + ', item_id: ' + item.id + ', column_id: "genre", value: "' + genre + '") { id } }'; await monday.api(mutation); console.log('Updated item:', item.name, 'with genre:', genre); } console.log('All genreless items updated.'); } catch (error) { console.error('Failed to update items:', error); } } updateItemGenres();
