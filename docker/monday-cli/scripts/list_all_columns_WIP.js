const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; async function listAllColumns() { try { const query = 'query { boards(ids: ' + boardId + ') { columns { id title type } } }'; const res = await monday.api(query); console.log('All Columns:', JSON.stringify(res.data.boards[0].columns, null, 2)); } catch (error) { console.error('Failed to retrieve columns:', error); } } listAllColumns();
