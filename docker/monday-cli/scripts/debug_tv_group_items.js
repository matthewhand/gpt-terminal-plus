const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; const groupId = 'new_group81872'; async function debugGroupItems() { try { const query = 'query { boards(ids: [' + boardId + ']) { id name groups(ids: ["' + groupId + '"]) { id title } } }'; const res = await monday.api(query); console.log('Debug Group Items:', JSON.stringify(res.data, null, 2)); } catch (error) { console.error('Failed to retrieve group items:', error); } } debugGroupItems();
