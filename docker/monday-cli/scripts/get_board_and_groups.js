const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; async function getBoardAndGroups() { try { const query = 'query { boards(ids: [' + boardId + ']) { id name groups { id title } } }'; const res = await monday.api(query); console.log('Board and Group Info:', JSON.stringify(res.data, null, 2)); } catch (error) { console.error('Failed to retrieve board and group info:', error); } } getBoardAndGroups();
