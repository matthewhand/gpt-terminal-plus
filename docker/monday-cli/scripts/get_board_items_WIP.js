const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; async function getBoardItems() { try { const query = 'query { boards(ids: [' + boardId + ']) { id name items { id name group { id } } } }'; const res = await monday.api(query); console.log('Full API Response:', JSON.stringify(res.data, null, 2)); } catch (error) { console.error('Failed to fetch items:', error); } } getBoardItems();
