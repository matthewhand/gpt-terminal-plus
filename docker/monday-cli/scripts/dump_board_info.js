const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); const boardId = 2809374317; async function dumpBoardInfo() { try { const boardQuery = 'query { boards(ids: [' + boardId + ']) { id name groups { id title } } }'; const boardRes = await monday.api(boardQuery); console.log('Full API Response:', JSON.stringify(boardRes, null, 2)); const board = boardRes.data.boards[0]; console.log('Board Info:'); console.log('- ID: ' + board.id); console.log('- Name: ' + board.name); console.log('- Groups:'); board.groups.forEach(group => { console.log('  - ' + group.title + ' (ID: ' + group.id + ')'); }); } catch (error) { console.error('Failed to dump board info:', error); } } dumpBoardInfo();
