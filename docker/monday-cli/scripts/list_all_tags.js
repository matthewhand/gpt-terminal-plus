const mondaySdk = require('monday-sdk-js'); const monday = mondaySdk(); monday.setToken(process.env.MONDAY_TOKEN); async function listAllTags() { try { const query = 'query { tags { id name } }'; const res = await monday.api(query); const tags = res.data.tags; console.log('All Tags:', JSON.stringify(tags, null, 2)); } catch (error) { console.error('Failed to retrieve tags:', error); } } listAllTags();
