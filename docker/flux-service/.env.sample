# Placeholder for the secure API token; replace with actual token
API_TOKEN=your_secure_api_token

# Set the environment to production
NODE_ENV=production

# Set the debug level for logging; adjust as needed
DEBUG=app:*

# Specify which GPUs the container should use; 'all' makes all GPUs visible
NVIDIA_VISIBLE_DEVICES=all

# Define the GPU driver capabilities; 'compute,utility' is typical for most tasks
NVIDIA_DRIVER_CAPABILITIES=compute,utility

FLUX_SERVICE_PORT=5020  # Example: Use port 5020 instead of the default 5017
GPT_TERMINAL_PORT=5001  # Example: Use port 5001 instead of the default 5000
NGINX_PORT=8081  # Example: Use port 8081 instead of the default 8080
