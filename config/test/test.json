{
  "local": {
    "protocol": "local",
    "hostname": "localhost",
    "code": false
  },
  "ssh": {
    "hosts": [
      {
        "hostname": "ssh-bash.example.com",
        "port": 22,
        "privateKeyPath": "/home/chatgpt/.ssh/id_rsa",
        "protocol": "ssh",
        "username": "chatgpt"
      }
    ]
  },
  "ssm": {
    "targets": [
      {
        "hostname": "GAMINGPC.WORKGROUP",
        "protocol": "ssm",
        "otherProperty": "value" // Replace with actual properties required
      }
    ]
  }
}
